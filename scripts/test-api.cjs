const fs = require('fs'); const env = fs.readFileSync('C:/Users/Shanmukh/OneDrive/Desktop/bts1/.env.local', 'utf8'); const key = env.match(/GEMINI_API_KEY=([\r\n]+)/)[1].trim(); async function run() { const res = await fetch('https://generativelanguage.googleapis.com/v1beta/models?key=' + key); const data = await res.json(); if (data.models) { console.log(data.models.map(m =; } else { console.log(data); } } run();
