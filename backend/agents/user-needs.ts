/**
 * Agent 3 — User Needs Personalization Agent (Unified Backend Re-export)
 */

import { UserNeedsAgent, userNeedsAgent } from '../../lib/agents/user-needs';

export { UserNeedsAgent, userNeedsAgent };
