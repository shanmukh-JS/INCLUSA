/**
 * Agent 5 — Verification Engine (Unified Backend Re-export)
 */

import { VerificationAgent, verificationAgent } from '../../lib/agents/verification-engine';

export { VerificationAgent, verificationAgent };
